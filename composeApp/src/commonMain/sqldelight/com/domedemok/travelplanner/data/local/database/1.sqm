-- Migration v1 → v2: add formerMembers column to TripEntity.
-- Existing rows on upgraded installs default to '{}' (no historical members).
ALTER TABLE TripEntity ADD COLUMN formerMembers TEXT NOT NULL DEFAULT '{}';
